<header class="navbar navbar-fixed-top navbar-inverse">
<div class="navbar-inner">
  <div class="container">
    <%= link_to "MMRC Helpdesk", new_ticket_path, id: "logo" %>
    <nav>
      <ul class="nav pull-right">
        <li><%= link_to "Home", root_path %></li>
         <% if signed_in? %>
            <li><%= link_to "Users", users_path %></li>
            <li><%= link_to "Tickets", tickets_path %></li>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                 <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
            <!--li>
              <!--form class="navbar-form navbar-right" role="search">
                <!--%= form_tag tickets_path,action: "search", method: :get  do %>
                  <!--%= text_field_tag :search, params[:search],placeholder: 'Search for ticket'%>
                  <!--%= text_field_tag :query, params[:query], {:class =>"search-query", :placeholder=>"Search"}  %-->
                  <!--%= submit_tag 'Search', :id=>"nav_search_btn" %-->
               
              <!--/form-->
                <!--div id="search">
                <--%= render :partial => 'tickets/search' %>
              </div>
            </li-->
             
          <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
          <% end %>
      </ul>
    </nav>
  </div>
</div>
</header>
